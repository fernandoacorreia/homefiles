#!/bin/bash
#
# SSH into a Domino Ubuntu instance.
#
set -o nounset -o errexit -o pipefail

echo "Trying with ubuntu" && ssh -i $HOME/.ssh/domino-test.pem ubuntu@$1 || echo "Trying with centos" && ssh -i $HOME/.ssh/domino-test.pem centos@$1
